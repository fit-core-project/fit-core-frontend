# Legacy Contracts

This folder keeps historical contract artifacts for reference only.

Files here are not active frontend runtime data, public API fixtures, or exercise naming SSOT. Active exercise muscle names must continue to follow the spreadsheet slug vocabulary from `fit-core-ai/scripts/exercise_tier.xlsx` or its CSV export.
